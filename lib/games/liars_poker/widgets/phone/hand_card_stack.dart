import 'package:flutter/material.dart';

class HandCardStack extends StatelessWidget {
  const HandCardStack({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
