# 2026-09 작업 기록

## 날짜별 목차

- [2026-09-07 — PACKAGE-MIGRATION-01](#package-migration-01)
- [2026-09-07 — GAME-COMM-DIAGNOSTICS-01](#game-comm-diagnostics-01)

## PACKAGE-MIGRATION-01

### 2026-09-07 — Flutter workspace 물리 패키지 분리

- 시작 기준: branch `feat/newstructure`, HEAD
  `90dc562a8897828e4b06a23218868d802529169b`. 작업 시작 전 worktree는 이미 사용자
  변경을 포함한 dirty 상태였고, porcelain snapshot SHA-256은
  `14a341ea424e96a726ed16792c428ed31a738f824f93d579b0b6399f55b94adc`였다. 기존 변경은
  reset·stash·stage하지 않았다.
- 구현: 루트의 core·Firebase·platform·게임 계약·공용 kit·게임 3종 소스 299개를
  `mosigame_core`, `game_contract`, `game_kit`, `mosigame_platform`,
  `game_liars_poker`, `game_final_call`, `game_mafia`로 물리 이동했다. 루트 `lib/`에는
  앱 부트스트랩·조립·게임 registry·앱 생성물만 남겼다.
- 에셋: 게임 3종과 core·kit·platform 리소스를 소유 패키지로 옮기고 패키지별
  FlutterGen을 생성했다. `GameImage`는 package 이름을 보존하고, 사운드는
  `packages/<package>/assets/...` 루트 번들 키를 유지한다. 서로 다른 패키지의 같은
  상대 경로는 다른 이미지 값으로 비교한다.
- 경계: `tool/check_package_boundaries.py`를 실제 루트와 `packages/*/lib` 스캔 방식으로
  바꾸고 금지 import, 미선언·역방향 pubspec 의존, 레거시 루트 소스 재등장을 검사한다.
  CI 허용치는 62건에서 0건으로 변경했다.
- 테스트·문서: asset 존재와 소유 pubspec 등록을 workspace 전체에서 확인하도록
  회귀 테스트를 확장했다. 패키지 stack frame 진단과 파일 위치 기반 마피아 테스트를
  새 경로에 맞췄고 Architecture, Package Migration, setup, 운영·게임 가이드의 현재
  경로를 갱신했다.
- 정리: Mafia 카드 에셋 아래에 섞여 있던 ignored `.dart_tool` 네이티브 캐시는
  `/private/tmp/mosigame-mafia-card-dart-tool-prepackage-20260907`로 옮겼다. 삭제하지
  않았으며 해당 임시 경로에서 복구할 수 있다.
- 검증 PASS(exit 0): `flutter pub get`; 패키지별 `dart run build_runner build`;
  깨끗한 ASCII 임시 복사본의 `flutter analyze --no-pub`; 경계 검사 `--max 0`;
  command retry 검사; Flutter 테스트 659개; Functions lint·build와 테스트 282개;
  임시 복사본의 `./gradlew assembleDebug`. 생성 APK에서 6개 asset 소유 패키지
  경로의 리소스 273개를 확인했다.
- 환경 이력: 한글 SDK 경로는 Gradle Java properties에서 깨졌고, Shorebird SDK로 바꾼
  첫 재시도는 공유 Gradle 캐시의 엔진 메타데이터 충돌로 실패했다. 저장소를 수정하지
  않고 `/private/tmp/mosigame-flutter-stable-sdk` ASCII symlink와 현재 Flutter SDK로
  Gradle을 직접 실행해 성공했다. Flutter 전체 테스트의 CLI 프로세스 2건은 PATH에
  `dart`가 없는 실행 환경에서만 실패했고, 명시적 SDK PATH로 재실행해 659개 전체가
  통과했다.
- 현재 한계: `dart run :mosigame validate --full`은 명시 승인 전 실행하지 않는 계약에
  따라 아직 실행하지 않았다. 실제 기기에서 게임별 이미지·사운드를 재생하는 수동
  확인과 다운로드 게임의 영구 cache root 배선·Shorebird 리허설도 남아 있다.
- 최종 targeted 검증 뒤 porcelain snapshot SHA-256은
  `cbb50482e4201c4f1a44e2142c0232e71af1adf4f967092f07304d6d1b15d45f`다. 시작 상태와
  달라진 것은 이 작업이 실제 소스·에셋을 이동하고 관련 기존 변경도 계속 포함한 결과며,
  staged 변경은 만들지 않았다.

### 2026-09-07 — 다운로드 게임 기반 배선

- 사용자 결정: 앱 진입에서는 Shorebird patch만 확인한다. 게임 에셋은 자동으로 받지
  않고, 향후 플레이어가 소유한 게임에 별도 다운로드 버튼을 제공한다. 이번 범위에는
  버튼·다운로드 화면, Firebase Storage 업로드·Rules, 실제 신작 배포를 포함하지 않는다.
- 구현: `path_provider`로 `<Application Support>/mosigame/games/` 영구 캐시를 앱 시작에
  연결하고 현재 적용된 Shorebird patch 번호를 저장소에 전달했다. core에는
  `GameAssetSource` 계약을, 앱 셸에는 `FirebaseGameAssetSource` 구현을 두어 Firebase
  SDK 의존이 캐시 로직으로 역류하지 않게 했다.
- 다운로드 계약: `requiredAssetVersion > 0`인 레지스트리 게임을 자동 등록하되 네트워크
  요청은 하지 않는다. 나중의 소유권 버튼만 `downloadGame`을 호출한다. Storage의
  매니페스트 gameId/assetVersion과 patch 요구 번호를 확인하고 각 파일을 `.part`로 받은
  뒤 SHA-256이 맞을 때만 노출한다.
- 재실행·진입: 검증에 사용한 매니페스트를 캐시에 함께 보존해 재실행·오프라인에서도
  다시 검증한다. 휴대폰 게임 진입, 태블릿 새 게임 시작과 복구는 설치된 파일 검증 후에만
  화면을 열며 이 과정에서 자동 다운로드하지 않는다.
- 관련 검증 PASS(exit 0): 에셋 매니페스트·캐시·store·bootstrap·호환성 및 휴대폰 대기·
  태블릿 선택 회귀 Flutter 테스트 68개, `python3 tool/check_package_boundaries.py --max 0`,
  Shorebird patch gate·화면 회귀 12개, `python3 tool/check_command_retry_contract.py`,
  `flutter pub get`. 동시 설치, 손상 파일 거부, patch 불일치, 시작 시 무다운로드,
  재실행 오프라인 캐시 재사용을 포함한다.
- 환경 한계: 원본 한글 경로의 `flutter analyze --no-pub`는 기존과 같은 analysis server
  LSP JSON `Unterminated string`으로 exit 255였다. 새 최종 후보의 Project CLI FULL은
  사용자 승인 전이므로 실행하지 않았다. 실제 Firebase Storage와 기기 검증도 후속이다.

## GAME-COMM-DIAGNOSTICS-01

### 2026-09-07 — 휴대폰·아이패드 게임 통신 타임라인

- 시작 기준: branch `feat/newstructure`, HEAD
  `90dc562a8897828e4b06a23218868d802529169b`. 패키지 분리를 포함한 기존 dirty
  worktree를 reset·stash·stage하지 않고 그대로 보존했다.
- 요청: 라이어스포커·파이널콜의 카드 제출, CALL, 룰렛 등이 끊기는
  시점과 이유를 휴대폰·아이패드 화면에서 직접 확인하게 한다.
- 구현: 앱 최상단 debug 진단 경계의 오른쪽 아래에 버튼을 추가하고, 최근
  200건·미확인 문제 배지·복사·초기화를 갖춘 타임라인을 배선했다. callable
  명령은 시작, 시도·재시도, 성공·보류·실패, 소요 시간, Firebase 오류 코드를
  남기고 RTDB는 연결 변경과 공개 `revision`·`phase`·`status` 수신을 남긴다.
  라이어스포커 룰렛은 서버 추첨, 4초 회전, 결과 반영 요청을 별도 시점으로
  기록한다.
- 보호: 방 코드·UID·카드·callable payload·개인 스냅샷 내용은 기록하지
  않고, release 빌드에서는 버튼·타임라인·`[game_comm]` 로그가 비활성화된다.
- 검증 PASS(exit 0):
  `flutter test --no-pub test/diagnostics/dev_error_log_test.dart test/diagnostics/game_communication_log_test.dart test/app_network_guard_test.dart test/callable_retry_policy_test.dart test/penalty_lever_sound_test.dart`
  — 관련 28개. 지정 파일 `dart analyze`는 `No issues found!`를 보고했다.
  `python3 tool/check_package_boundaries.py --max 0`은 경계 위반 0건,
  `python3 tool/check_command_retry_contract.py`는 모든 호출의
  `commandId`/`interruptionId` 재시도 계약 통과를 보고했다.
- 환경 한계: `flutter analyze --no-pub`는 소스 진단 전에 analysis server가 LSP
  initialize JSON을 끝까지 읽지 못해 `FormatException: Unterminated string`으로 종료했다.
  코드 문제와 구분하기 위해 동일 파일을 Dart analyzer로 다시 확인했다.
- FULL 검증: 사용자 승인 후 Node 22를 명령 PATH에만 적용해
  `dart run :mosigame validate --full`을 실행했다. 원본 한글 경로에서는 Flutter
  analysis server의 LSP JSON `Unterminated string`이 재현됐고, 같은 710개
  working-tree 상태를 `/private/tmp`의 ASCII 경로에 복사해 lockfile 기준
  의존성을 재생성한 뒤 최종 **PASS(exit 0)**했다. preflight, Dart format,
  Flutter analyze·전체 664개, Functions lint·전체 282개, working-tree 보존의
  7단계가 모두 통과했다.
- 배포: 승인된 범위로 `project0000-ec01e`의
  `game_liars_poker_prepare_penalty`와 `game_liars_poker_resolve_penalty`만 지정했다.
  전역 Firebase 실행 파일은 ESM 분석 단계에서 배포 전 종료됐고, 저장소의
  `firebase-tools` 14.27.0을 Node 22로 실행한 재시도는 predeploy lint·build,
  신규 함수 생성, 기존 함수 갱신을 모두 완료해 **Deploy complete(exit 0)**했다.
  배포 후 목록에서 두 함수 모두 v2 callable, `asia-northeast3`, Node.js 22로
  확인했다. 다른 함수·DB rules·RTDB 데이터는 배포하거나 변경하지 않았다.
- 남은 검증: 실제 휴대폰·아이패드 debug 빌드에서 룰렛 서버 추첨·
  4초 회전·결과 반영과 진단 기록 복사를 재확인한다.

#### 2026-09-07 후속 — 레버 즉시 회전 회귀 수정

- 증상: 서버 추첨 함수 배포 후 룰렛 레버를 내려도 원판이 잠시
  정지했다가 서버 응답 후 회전했다.
- 원인: 이전 클라이언트 추첨은 레버 연출 220ms 후 바로 `rollTo`를
  시작했지만, 서버 권한 추첨으로 바꾸며 `onPrepareResult()`를 먼저
  `await`해 네트워크 응답 시간이 정지 구간으로 노출됐다.
- 수정: 레버 연출이 끝나면 서버 추첨과 동시에 `rollInfinitely`로
  즉시 회전한다. 결과 수신 후에는 4초 전체 연출의 남은 시간(최소
  1.2초)동안 해당 칸으로 감속한다. 서버가 결과·확률을 결정하는
  계약은 변경하지 않았다.
- 진단: 서버 응답 기다림 시작과 응답 소요 시간, 목표 칸 감속
  시작을 실제 순서로 표시한다.
- 검증 PASS(exit 0): 변경 파일 Dart 정적 분석, `penalty_lever_sound_test.dart`,
  `liars_poker_rotation_test.dart`, `sound_start_offset_test.dart`,
  `game_communication_log_test.dart` 총 21개. 신규 테스트는 서버 Future를
  미완료 상태로 유지한 동안 원판 각도가 실제로 변하는지 확인한다.
- 남은 검증: 새 최종 후보에 대한 Project CLI FULL은 사용자 승인 전이며,
  실제 아이패드에서 레버 즉시 회전·감속 연결·사운드 정렬을 확인해야 한다.
